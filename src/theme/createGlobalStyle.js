import { createGlobalStyle } from 'styled-components';

export const GlobalStyle = createGlobalStyle`
	html {
		background: white;
	}
`
throw new Error('This will require <GlobalStyle /> component. Do that, then delete this error message.')
